server:
  http_port: 8080
  https_port: 8443

security:
  jwt_secret: "secret"
  certificate: "test-cert.pem"
  private_key: "test-key.pem"

database:
  host: "localhost"
  port: 5432
  username: "postgres"
  password: "password"
  database_name: "actix-web-demo"
